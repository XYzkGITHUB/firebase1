
{
  "entities": {
    "Lead": {
      "title": "Lead",
      "description": "Information about a potential client from the feedback form.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Client name"
        },
        "phone": {
          "type": "string",
          "description": "Phone number"
        },
        "section": {
          "type": "string",
          "description": "The site section where the request was left (e.g., keramogranit, laminate_sps, delivery)"
        },
        "description": {
          "type": "string",
          "description": "The text of the message or request description"
        },
        "type": {
          "type": "string",
          "enum": ["feedback_or_bug"]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the lead was created"
        }
      },
      "required": [
        "name",
        "phone",
        "section"
      ]
    },
    "VerificationCode": {
      "title": "VerificationCode",
      "description": "Temporary OTP for email verification or password reset.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email"
        },
        "code": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": ["registration", "password_reset"]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "email",
        "code",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "email_password"
    ]
  },
  "firestore": {
    "/leads/{leadId}": {
      "schema": "Lead",
      "description": "Collection of incoming leads. PUBLIC CREATE ACCESS IS REQUIRED."
    },
    "/verificationCodes/{codeId}": {
      "schema": "VerificationCode",
      "description": "Temporary storage for OTP. PUBLIC CREATE AND READ (WHERE EMAIL AND CODE MATCH) ACCESS IS REQUIRED."
    }
  }
}
